TIL

26/9/4
-테이블 간 관계는 1:1, 1:N, N:M 세 가지 유형으로 구분되며, 실무에서 자주 쓰이는 N:M 관계는 중간 테이블을 통해 두 개의 1:N 관계로 분리함.
-ERD는 이러한 테이블 간 관계와 구조를 까마귀발 표기법 등의 기호를 사용하여 시각적으로 표현한 다이어그램임.
-ERD를 파악할 때는 중심 엔티티에서 출발하여 연결선 끝의 기호를 확인하며 관계의 필수 여부와 수량을 체계적으로 읽음.

26/9/3
-데이터 사이언티스트는 데이터베이스에서 필요한 정보를 직접 추출·가공하기 위해 SQL 역량이 필수적임.
-관계형 데이터베이스(RDBMS)는 테이블(행·열) 형태로 데이터를 저장하며, 스키마와 데이터 타입을 통해 규격화된 구조를 가짐.
-PK와 FK를 활용한 참조 무결성 유지 및 정규화를 통해 데이터 중복을 줄이고 일관성과 안전성을 보장함.

26/9/1
- 기존 Seq2Seq 모델은 긴 문장을 고정된 크기의 벡터 하나에 압축하면서 정보가 유실되는 병목 현상과 장기 의존성 문제가 존재함.
어텐션 메커니즘은 단어를 생성할 때마다 인코더의 모든 은닉 상태를 참조해 필요한 정보에 선택적으로 집중함으로써 이를 해결함.
Query와 Key의 유사도로 가중치를 구한 뒤 Value와 가중합하여, 현재 예측에 필요한 동적 컨텍스트 벡터를 생성하는 구조임.

26/8/31
-자연어 전처리 과정에서 고차원 희소 데이터인 원핫 인코딩의 한계를 극복하기 위해, 단어의 의미와 맥락을 저차원 밀집 벡터로 압축하는 임베딩 레이어의 원리 한 장기 의존성 문제를 해결한 LSTM 아키텍처를 활용해 시퀀스 데이터를 처리하고, PyTorch로 감성 분류 모델과 조기 종료 학습 루프를 구현하는 방법을 공부했습니다.

26/8/28
-선형 회귀의 출력값을 활성화 함수 시그모이드 & 소프트맥스를 통해 0과 1 사이의 확률로 변환하여 분류 문제를 해결하는 로지스틱 회귀의 작동 원리와 크로스 엔트로피 손실 함수를 설명합니다. 또한, 범주형 데이터의 수치적 편향을 막는 원핫 인코딩 전처리 및 차원의 저주 대안(SCCE 등)을 다루며, 데이터 선택 편향을 방지하기 위한 K-Fold 교차 검증의 필요성을 학습했습니다.

26/8/27
-제공된 학습 자료는 데이터의 비례 관계를 최적의 직선 방정식 y = wx + b 으로 모델링하여 미래 값을 예측하는 선형 회귀의 기본 원리와 오차 평가지표(MSE, MAE)의 특성을 설명합니다. 또한, 비용 함수를 최소화하는 경사하강법 최적화 원리와 오버슈팅 방지를 위한 데이터 스케일링, 과적합을 막기 위한 데이터 세트(Train/Valid/Test) 분할의 중요성을 배웠습니다.

26/8/26
-선형 회귀는 데이터 간의 관계를 모형화한 직선의 기울기(와 절편을 학습하여 미래의 값을 예측하는 기초 머신러닝 기법입니다. 모델의 성능을 평가할 때 MSE는 이상치에 큰 벌점을 주어 최적화에 유리하며, MAE는 실제 값과 동일한 단위를 가질 수 있어 오차를 직관적으로 해석하는 법을 학습했습니다.

26/8/25
-머신러닝의 기본 개념과 데이터 분석 기초 과정을 배웠습니다. 머신러닝은 사람이 규칙을 짜는 대신 데이터에서 컴퓨터가 스스로 패턴을 학습하는 기술로, 정답 유무와 형태에 따라 지도, 비지도, 강화 학습으로 나뉩니다. 데이터 분석 기초에서는 결측치를 평균이나 최빈값으로 채우고 이상치와 중복 데이터를 정제하는 방법을 익혔으며, Pandas의 .loc 를 활용한 안전한 데이터 수정과 Matplotlib, Seaborn을 통한 히스토그램 박스 플롯 산점도 시각화 기법을 다루었습니다.

26/8/24
-인공지능으로부터 정교한 결과물을 도출하기 위한 프롬프트 엔지니어링의 핵심 기법을 배웠습니다. 예시 없이 지시만 내리는 제로샷과 달리 퓨샷 프롬프팅은 2~5개의 모범 예시를 함께 제공하여 정확한 출력 패턴과 톤앤매너를 학습시킨다는 점을 확인했습니다. 또한 생각의 사슬(CoT) 기법을 활용해 단계별 추론 과정을 거치도록 유도하면 복잡한 논리나 수리 문제에서 오류를 대폭 줄일 수 있음을 익혔습니다. 나아가 역할(Role), 목적(Goal), 맥락 및 제약조건(Context/Constraint)을 세우는 RGC 프레임워크로 프롬프트를 구조화하는 법과, AI가 참조하는 전체 정보 환경을 설계하여 환각 현상을 방지하고 답변의 질을 극대화하는 컨텍스트 엔지니어링의 중요성을 다졌습니다.

26/8/21
-동기/비동기 및 블로킹/논블로킹의 개념과 이벤트 루프의 동작 원리를 파이썬의 코루틴 및 표준 라이브러리 실습을 통해 다지며 효율적인 비동기 I/O 처리 방식을 배웠습니다. 또한, 규칙 기반 프로그래밍에서 데이터 기반 학습으로 진화해 온 인공지능의 발전 과정과 함께 AI, 머신러닝, 딥러닝, 생성형 AI(LLM) 간의 계층적 구조 및 트랜스포머 아키텍처의 핵심 개념을 학습했습니다.

26/8/20
-오늘 파이썬 표준 라이브러리(datetime, time, math, random, os, sys)를 활용해 시간 측정, 수치 연산, 난수 생성 및 시스템 제어 기초를 배웠습니다. 이어서 with open() 구문을 활용해 자원 누수 없이 텍스트 파일을 안전하게 읽고 쓰는 다양한 방법을 익혔습니다. 또한 CSV와 JSON 데이터를 파이썬 내장 자료구조인 리스트와 딕셔너리로 상호 변환하는 기술을 학습했습니다. 나아가 dump와 load 함수를 이용해 데이터를 텍스트 파일로 저장하고 원상 복원하는 직렬화 및 역직렬화 과정을 실습했습니다. 이번 학습을 통해 내장 모듈 활용 능력과 데이터 파일 입출력 실무 기초를 탄탄히 다졌습니다.

26/8/18
- 객체지향과 클래스, 인스턴스의 관계,  생성자와 self 키워드로 객체 제어하기, 상속과 메서드 오버라이딩에 대해 배웠습니다

26/8/14
- 이터러블 , generator, module의 개념에 대해 배웠으나. 하나 흥미로운 점은 비거깅 하면서 튜플과 float을 연산을 못 한다는 점이였습니다. 따라서 튜플을 하나의 정수로 전화시켜 연산을 해야한다는 점 

26/8/13
-변수의 유효 범위와 LEGB 스코프 구조 안에 전역 변수 와 지역 변수의 작동 방식을 배웠습니다

26/8/12
- 변수 매개변수 와 인수의 차이를 배웠습니다. 또한 변수 와 컴퓨터가 어떤 방식으로 소통하는지에 대한 이론적인 것을 배웠습니다.

26/8/11
- 파이썬 조건문: 기본 문법, 규칙, 양자텍일. 반복문 : for & range 및 반복문의 중간 제어에 대해 배웠습니다. 

26/8/10
- 시큐엔스 자료에 가장 흔히 사용되는 리스트와 튜플에 대해 배웠습니다

26/8/7
- 기본 자료, 연사 종류, 문자열 형식화. 마지막으로 주의 사항들

26/8/6
- python 환경 설치법 및 python 기초인 용어 및 원리에 대해 배웠습니다.

26/8/5
- 저장소 구성 및 관리에 중점이 있었습니다. 협업에 중요한 켄베션, 문서 작성 및 Github의 기능들이 중점 이였습니다.  

26/8/4
- 제가 오늘 이 TIL을 github에 성공적으로 올리면 TIL의 80%에 이해도 와 복습이 되것 입니다.
